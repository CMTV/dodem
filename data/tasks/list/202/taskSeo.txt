Доказать, что если для показательной функции f() = a^x (a > 0) значения аргумента x = x_n образуют арифметическую прогрессию, то соответствующие значения функции y_n = f(x_n) образуют геометрическую прогрессию.