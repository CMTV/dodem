Доказать, что если для линейной функции f() = ax + b значения аргумента x = x_n образуют арифметическую прогрессию, то соответствующие значения функции y_n = f(x_n) образуют также арифметическую прогрессию.